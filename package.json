{
  "name": "Learning",
  "private": true,
  "scripts": {
    "build": "npm --prefix ionic-app run build -- --configuration production",
    "ionic:build": "npm --prefix ionic-app run build -- --configuration production"
  }
}
